import React from 'react';

const person = (props) => {
  return (
    <div>
      {/*<p>{props.name} - {props.days} days left</p>*/}
      <p>{props.name}</p>
    </div>
  )
};

export default person;